# 更新日志

## [0.3.9] - 2026-09-03

### 修复

- 微信输入法设置恢复为与微信输入法 2.1.3.18 一致的两种模式：“启动语音输入”使用左 Ctrl + 左 Win 点击触发，“按住说话”使用左 Ctrl + 左 Shift + D 按住触发；软件内一次只应用一种。
- 删除不存在的 F5 + 左 Ctrl + 左 Win 设置说明；旧 `wechat-hold` 配置会在读取或保存时自动迁移到推荐的 `wechat-current` 按住说话模式，手动自定义快捷键保持不变。
- ATVV 语音按压增加明确的边沿去重测试，重复 DOWN/UP 不再重复进入快捷键状态机；原有固件 F5/修饰键过滤、WinUHid 和 SendInput 降级路径继续保留。

### 验证边界

- 前端 47/47、Rust 120/120、前端生产构建、Cargo workspace 全目标检查和 Windows Tauri 打包通过；GitHub 正式 Release `v0.3.9` 已公开，NSIS 安装包及 `latest.json` 均已上传，远端摘要、清单端点和公开下载复核通过。微信两个模式仍需在真实遥控器和微信输入法中分别验收。

## [0.3.8] - 2026-09-03

### 修复

- 应用更新检查改为优先读取 Release 更新清单，不再把 GitHub API 的匿名额度耗尽（403）直接当作必须重启才能恢复的错误；保留仓库清单和 GitHub API 兼容回退。
- 自动检查开启后会在启动、每 6 小时以及窗口恢复且到期时执行；焦点和页面可见性事件会去重。
- 手动“检查更新”与后台检查重叠时会等待同一请求，不再错误显示“已是最新版本”。GitHub 限流、网络、HTTP、解析和校验失败会显示可重试说明并写入诊断日志。

### 验证边界

- 前端 47/47、Rust 116/116、生产构建、Cargo 全目标检查和 Windows Tauri 打包通过；v0.3.6/v0.3.7 仍使用旧 GitHub API 路径，遇到额度耗尽时需手动安装 v0.3.8 作为一次性升级。

## [0.3.7] - 2026-09-03

### 新增与修复

- 微信输入法改为唯一可见的 `wechat-hold` 预设：软件真实按住左 Ctrl + 左 Win；旧 `wechat` 和 `wechat-current` 配置仅保留读取兼容，不会自动迁移。
- 语音 F5 回退链路补充放行 DOWN/UP 配对和异步注入分派，减少实体 F5 被误吞与键盘钩子阻塞风险。
- 电池沿用 Nexus Prime 图标与充电动画，新增绿（≥30%）、黄（10%–29%）、红（<10%）和未知灰色空电池状态；后端电量更新会即时同步页面。
- 新增 VB-CABLE 官方 Pack45 的受限应用内下载：另存为、进度、取消、16 MiB 限制、HTTP/长度/ZIP/SHA-256 校验以及原有目标文件保护；下载后只提示手动安装。

### 验证边界

- 自动化检查通过；微信输入法、实体 F5、WinUHid/SendInput 和真实设备电量通知仍需真机验收。

## [0.3.6] - 2026-08-28

### 修复

- 普通 Enter 与上下左右映射改为带应用标记的 SendInput 路由，避免它们再次被本应用的特殊键钩子当作遥控器原始事件吞掉；短按、长按连发和鼠标映射保持原路径。
- 语音会话在 ATVV 事件和目标快捷键之间短暂过滤遥控器固件泄漏的左 Ctrl、左 Win；关联 F5 到达后立即结束按下过滤，并收紧抬键放行窗口，避免微信 `左 Ctrl + 左 Shift + D` 被固件抬键提前打断。
- 输入法预设持久化新增可选 `voice_input_profile`。手动修改语音快捷键或触发模式会清空该标记；按住期间锁定预设、快捷键与注入路由。千问右 Alt 松开改为同路由直接释放，不再插入 F24 或额外发送 Enter/Space，以保留输入法的自动上屏。
- 托盘专用 `tray-icon-compact.png` 保持黑色圆角底与白色 `N.` 轮廓，仅扩大主体可见尺寸；窗口、安装器、桌面及其他平台图标不变。

### 验证边界

- 自动化验证、安装包元数据和本地覆盖安装结果见 `HANDOFF.md` 本轮记录。
- 微信、千问、真实遥控器方向/确认、鼠标与 Windows 隐藏托盘面板均需要在实际设备上验收，不以单元测试替代。

## [0.3.5] - 2026-08-28

### 优化

- 微信输入法预设文案与触发模式对齐：
  - `wechat`（启动语音输入，左 Ctrl + 左 Win）触发模式由「按住」改为「点击」，描述为「按下启动语音，按任意键结束（软件只发一次脉冲）」；
  - `wechat-current`（按住说话，左 Ctrl + 左 Shift + D）描述为「按住可语音输入，松手结束」；
  - `applyHint` / `logMessage` 同步微信输入法原生措辞。
- 微信设置面板摘要与「启动语音输入」说明同步上述措辞。
- 回归测试 `imePreset.test.ts` 的 `trigger_mode` 断言同步为 `Toggle`。

## [0.3.4] - 2026-08-27

### 修复

- 修复 v0.3.3 中旧版微信「松开结束」点按的确定性自死锁：停止点按成功后在持锁状态下对同一把非重入锁再次加锁，导致语音键第一次松开时钩子线程即卡死；随后「修复虚拟键盘」「修复语音连接」「重启按键桥接」等快捷操作全部永久停在“处理中”。现在会话的停止点按与清空经由同一守卫完成，不再重入加锁；停止失败时会话保留以便重试，并新增回归测试覆盖。

## [0.3.3] - 2026-08-27

### 修复

- 旧版微信 Ctrl + Win 语音映射改为「按下启动、松开结束」的两次独立点按：不再全程持有组合键，避免遥控器固件连发的 F5 与 Ctrl + Win 组合被微信再次识别、反复弹出语音窗。
- 点按会话沿用首次成功的注入路由完成开始与结束两半；结束失败时同路由重试并执行紧急释放，配置变更或断开连接也能正确收尾。

### 新增

- 全局设置新增「清理旧日志」按钮：确认后删除历史日期与旧格式运行日志，保留当天日志继续排障，并显示清理数量与释放空间。

### 优化

- 首页在宽屏下三张卡片纵向弹性铺满窗口，窄屏维持原有滚动布局。
- 语音 F5 抑制改为一轮会话汇总一条日志，不再逐条刷屏。

## [0.3.2] - 2026-08-27

### 优化

- 首页服务状态卡片压缩为 68px 扁平四列布局，快捷操作改用短标题与单行说明；简体、繁体与英文文案同步。
- 设备状态、页面连接状态、服务状态、实时提示与动态时间线统一使用 7px 实心圆加 4px 半透明外环的状态灯，不再使用方形提示或闪烁动画。
- 非语音按键的鼠标操作收为下拉菜单，支持鼠标左键及四向移动、步幅调节、长按加速与已有参数回填。
- 旧版微信 Hold Ctrl + Win 的释放不再回放裸组合键，虚拟 HID 与 SendInput 路径均先经 F24 中和再逐键抬起。
- 托盘改用紧凑专用图标，16px 下有效不透明主体明显增大，不影响窗口、安装器或网页图标。

## [0.3.1] - 2026-08-27

### 新增

- 按键映射新增鼠标操作：可为任意非语音键的单击、多击或长按槽位设置鼠标左键及上下左右移动。默认方向键仍输出键盘方向键。
- 鼠标移动支持 1–100 像素步幅；绑定到长按槽位时可启用连续加速移动，松开按键、断开连接或保存配置会立即停止。

## [0.3.0] - 2026-08-27

### 修复

- 语音 Click/Hold 在 `AUDIO_START` 时立即按下输入法快捷键；Click 仅在抬起时保证最短按住时长，避免输入法悬浮语音条延迟或时机异常。
- 组合键释放改为保留持键状态直至确认成功；WinUHid 失败后执行同路由重试和应用持有键的紧急 KEYUP，修复 Ctrl+Win、右 Alt 等可能粘键的问题。
- 纯 Win/Alt 修饰键语音快捷键释放前发送 F24 中和键，减少空按时弹出 Windows 开始菜单或系统菜单。

### 优化

- 运行日志改为按日后台写入，记录界面、设置、命令、设备、按键和语音会话等语义事件；默认保留 7 天，可在设置中调整，单日大于 10MB 自动分段保留。

## [0.2.9] - 2026-08-27

### 修复

- 修复 WinUHid 根设备创建时错误调用 ANSI SetupAPI 接口的问题。此前 Windows 会把 `Root\\WinUHid` 拆成多个单字符硬件 ID，驱动虽已加入驱动仓库却无法匹配和启动，导致首页“修复虚拟键盘”失败。
- 修复驱动绑定流程：安装器现在在已创建的根设备上直接选择并执行兼容驱动安装，再扫描硬件变更并实际打开 `\\.\\WinUHid` 验证成功；不再把“驱动已暂存”误报为修复成功。
- 首页强制修复会清理由旧版脚本创建的、且硬件 ID 被拆分的无效 WinUHid 根设备；清理范围严格限定为该错误签名，不涉及其它设备。
- 安装阶段日志写入应用数据目录，提升权限的子进程失败时会把最后一条原始原因返回给应用，方便定位 UAC、驱动包或 SetupAPI 故障。

## [0.2.8] - 2026-08-26

### 修复

- 语音快捷键的点击、按住和点击后长按三条路径均优先使用 WinUHid；按下后会固定使用同一路由释放，WinUHid 不可用时才回退 SendInput。
- 恢复 Alt 组合、空格和音量/静音的既有按键分流，避免虚拟键盘修复影响普通遥控器映射。
- 虚拟键盘修复会先释放正在按住的语音快捷键，并区分已就绪、需要重启与安装失败状态。
- 开机启动改用当前用户 Run 注册表，并清理旧版 Startup 快捷方式；只有从 Windows 登录自启时才会按设置最小化到托盘，手动启动始终显示主窗口。
- 端口冲突检测可正确解析 TCP/UDP `netstat` 输出，并避免后台命令弹出控制台窗口。

### 优化

- 首页五项快捷操作采用完整响应式卡片布局；所有修复操作在执行期间互斥。
- 驱动文件、安装脚本与 SDK DLL 均随正式安装包部署；首次启动自动检测并请求安装，之后可从首页强制修复。

## [0.2.7] - 2026-08-26

### 修复

- 正式安装包内嵌 WinUHid 虚拟键盘驱动与首次启动安装流程；豆包、微信等会过滤普通模拟按键的输入法快捷键现优先以虚拟 HID 键盘发送。
- 首页“快捷操作”新增“修复虚拟键盘”，并提供适用场景、修复内容与管理员确认提示。

## [0.2.6] - 2026-08-25

### 优化

- 全面替换 Nexus Prime 图标：主窗口、任务栏、系统托盘、安装器、卸载器、网页 favicon 与平台图标资源同步更新为深色面板、白色 `N.` 和彩色霓虹边框。
- 图标外围黑底改为真实透明通道，保留收窄的半透明彩色光晕，提升浅色和深色桌面背景下的显示效果。

## [0.2.5] - 2026-08-25

### 优化

- 主窗口隐藏至托盘时同步暂停 WebView2 渲染，恢复窗口时重新显示并聚焦，降低后台资源占用。
- 音频路由和 VB-CABLE 环境状态加入短时缓存，减少后台设备枚举与状态探测。

### 修复

- 音频环境修复后立即失效并刷新缓存；瞬时探测错误不再被缓存为“未安装”。
- 修复并发探测可能覆盖修复结果、驱动压缩包路径失效及托盘恢复窗口可见性异常。

## [0.2.4] - 2026-08-19

### 修复

- 微信输入法语音预设恢复旧版的“左 Ctrl + 左 Win”，并新增新版“按住说话”的“左 Ctrl + 左 Shift + D”。设置页会同时展示两个选项，用户可按输入法实际版本选择，不再以新版预设覆盖旧版。

## [0.2.3] - 2026-08-19

### 修复

- 微信输入法升级后，旧版“左 Ctrl + 左 Win”语音预设不再能唤起语音输入。预设现改为微信输入法“按住说话”的左 Ctrl + 左 Shift + D，按下遥控器开始听写、松开结束。
- 更新微信输入法设置说明，移除已过期的快捷键示意，明确提示在微信输入法“设置 → 语音输入”启用“按住说话”；自定义快捷键仍可手动录入。

## [0.2.2] - 2026-08-18

### 修复

- 遥控器预览回归 0.1.8 原版实现（0.1.9~0.2.1 的三次改动全部回退）：`aspect-ratio: 401/1919` 相对元素自身宽度，任何窗口宽度下容器比例恒等于遥控器图片，蓝框热点与按钮精确对齐、不错位、不塌陷。

## [0.2.1] - 2026-08-18

### 修复

- 遥控器预览热点错位（0.2.0 引入）：按键映射页窄窗口（媒体查询宽度 75/78px）下，预览容器固定高度与图片比例失配，`object-fit: contain` 产生上下留白导致蓝色热点与按钮错位。改为高度随宽度联动（padding-top 百分比 = 1919/401），任何宽度下与图片精确对齐。

## [0.2.0] - 2026-08-18

### 修复

- 遥控器预览尺寸回归（0.1.9 引入）：按键映射页的遥控器示意图被压缩成细条，恢复为固定 402px 高度，全版本引擎显示一致。

## [0.1.9] - 2026-08-18

### 修复

- 开机后语音键失灵（输入法不唤起）：HID Tap 注入与 ATVV 语音通道并发抢占 WUDFHost 导致语音通道失效、只能重启电脑恢复。现在 HID Tap 附着以 ATVV 订阅成功为前提，ATVV 失败时等待后台重试窗口（15 秒）再附着，语音键优先级最高。
- HID Tap 宿主进程查找日志降级，不再每秒刷屏撑爆日志文件。
- Win10 界面兼容：
  - 窗口默认尺寸超出小屏/高缩放工作区（如 1366x768 @125%）时自动收敛并居中，底部内容不再被裁。
  - `inset` 简写替换为 `top/right/bottom/left`，修复旧版 WebView2 下运行日志模块丢失。
  - `100dvh`、`color-mix()`、`aspect-ratio` 增加旧版 WebView2 兼容回退；日志区增加最小高度兜底。
  - 非常规 `font-weight`（650/720/730/740/750/760/770/780）标准化为 600/700/800，消除 Win10 字体取整差异。

## [0.1.8] - 2026-08-16

### 新增

- 按键录入支持特殊键与小键盘：Pause、CapsLock、PrtSc、Insert、Menu、NumLock、ScrLk、小键盘 0-9 与运算键、标点符号、F13-F24，录入后显示正确的键名。
- 新键的 `voice_hotkey` 配置使用可读名字（如 `numpad0`、`pause`、`capslock`），旧配置 `vk_xx` 格式仍可正常回读。
- 媒体键录入兜底：媒体键（音量 +、音量 -、静音、应用 2）录不上时可直接点击设置（非语音键）。

### 优化

- 映射列表快捷键胶囊自适应换行：长组合键不再截断遮挡，完整显示。

## [0.1.7] - 2026-08-13

### 新增

- 豆包输入法预设支持右 Alt 长按语音与右 Alt + 空格免按语音两种模式。

### 优化

- 未连接设备时统一显示短状态和红色状态点，并清除历史设备信息。
- 按键映射页连接状态区域在窄窗口下可换行，不再挤压标题与说明。

## [0.1.6] - 2026-08-13

### 修复

- 语音键改为专用语音快捷键，彻底移除前端入口和后端扩展手势分支，避免旧手势覆盖输入法语音快捷键。
- 自动清理历史语音键的双击、三击、四击和长按映射；其他遥控器按键的手势设置不受影响。
- 微信输入法映射固定走左 Ctrl + 左 Win；异常路径会释放实际按下的快捷键，避免右 Alt 等按键残留。
